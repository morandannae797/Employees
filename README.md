# Employees
hecho checar : dtos, junto con manual mapping, checar si queremos usar automapper, FLuent API(teoricamente es el espacio de fluent configuration pero checar)
#cambio pendientes :  
Checar como se haran las migraciones? en nuestro caso con correr el scaffold otra vez, 
que hacemos con el connection string
>can create our services, first we create the interfac:

    one for read only operations
    another for all operations
    esto debemos de unificarlo con el generic repoy hacerle el mapeo
es igual faltante el dependency injection, CQRS y appsettig,  obvio lo que el controlador hara
